import socket 

s = socket.socket(socket.AF_INET, socket.SOCK_STREAM) 
s.connect(('httpbin.org', 80)) 
s.send("request_text") //Например: GET /ip HTTP/1.1\nHost: httpbin.org\nAccept: */*\n\n 

while True: 
r = s.recv(1024) 
if r == "": 
break 
print(r) 
s.close()